#attempt 1: need list of files first, bu then what? accessing ies file
from os import listdir
mypath="/Users/DeMates..."
list_of_filenames=listdir(mypath)
for filename in list_of_filenames:
  print filename
  <then run script to extract the data we want from each file?>

#how to move from printing filename to using? Is there something in between importing and writing csv?
#writing csv file

#attempt2: using pandas- nope
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
